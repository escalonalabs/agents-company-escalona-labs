# Kernel Docs

This directory defines the deterministic kernel contracts for `M1 Kernel v1`.

## Documents

- `canonical-domain-model.md` maps to `AC-101`
- `runtime-state-machine-and-event-schema.md` maps to `AC-102`
- `replay-idempotency-and-retry-semantics.md` maps to `AC-103`
- `persistent-ledger-and-materialized-projections.md` maps to `AC-104`
- `golden-trace-harness.md` maps to `AC-105`

## Reading order

1. canonical domain model
2. runtime state machine and event schema
3. replay, idempotency, and retry semantics
4. persistent ledger and materialized projections
5. golden trace harness
